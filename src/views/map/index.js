import React, {useEffect} from 'react'

import SvgIcon from '@/components/SvgIcon'

const GDMap = () => {
    return (<section>
        <SvgIcon name='back'/>
        <div>高德</div>
    </section>)
}

export default GDMap
